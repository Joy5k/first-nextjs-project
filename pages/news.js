import styles from '@/styles/Home.module.css'
const News = () => {
    return (
        <div  className={`${styles.main}`}>
            <h2>This is news page</h2>
        </div>
    );
};

export default News;